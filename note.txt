
3945行到5393行，是Sizzle选择器框架，可以当成一个独立框架使用。目前，这部分已经抽离成一个独立的项目（框架）。可以查一下这个项目的资料。
 */  //# Sizzle选择器框架，可以当成一个独立框架使用。
(function(){

js是函数作用域。在函数里定义的变量都是局部变量，这样就很好的避免了过多的全局变量(jQuery仅仅2个全局变量jQuery和$)。由于闭包属性，虽然函数自执行结束了，但自执行函数里面定义的局部函数和变量还是能够被定义成全局变量的jQuery和$所引用到，类似于Java的私有变量。好处可见一斑。
问题：jQuery中，难道没有闭包问题吗？！这个问题，jQuery是如何解决的呢？
